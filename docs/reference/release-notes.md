# Release notes

The repository [CHANGELOG](../../CHANGELOG.md) is the canonical release-history document.

The current development line is 0.1.1-SNAPSHOT.

This documentation is versioned with the source tree. When reading documentation on a feature branch, treat it as describing that branch rather than a published artifact.

Before consuming a non-SNAPSHOT dependency, confirm the release/tag and artifact repository published for that version instead of copying a development coordinate from this branch.
