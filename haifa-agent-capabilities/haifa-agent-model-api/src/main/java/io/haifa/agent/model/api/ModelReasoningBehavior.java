package io.haifa.agent.model.api;

/** Stable reasoning behavior exposed by a reviewed model binding profile. */
public enum ModelReasoningBehavior {
    NONE,
    OPTIONAL,
    ALWAYS,
    ADAPTIVE
}
