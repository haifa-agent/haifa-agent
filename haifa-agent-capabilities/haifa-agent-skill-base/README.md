# Haifa Agent Base Skills

This module packages the deliberately small SDK-level Skill set distributed with Haifa Agent.
It contains only `task-planning` and `result-verification`.

Both Skills are read-only classpath resources. They contain instructions only: no scripts,
network access, credentials, or Tool grants.
