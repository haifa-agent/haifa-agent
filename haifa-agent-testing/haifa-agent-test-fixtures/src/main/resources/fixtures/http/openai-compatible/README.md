# OpenAI-compatible HTTP Fixtures

Small deterministic response bodies for loopback Stub HTTP Servers. They contain no real prompt, credential,
provider response, account identifier, or production data.

Tests must still exercise the production HTTP adapter and JSON mapping; this fixture is not a replacement for
the adapter itself.
