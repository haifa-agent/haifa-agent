package io.haifa.agent.personalassistant.application.mission;

/** Explicit product mode. Deep Research is never inferred from free-form user text. */
public enum MissionMode {
    STANDARD,
    DEEP_RESEARCH
}
