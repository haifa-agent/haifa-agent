package io.haifa.agent.project.hostworkspace.directory;

/** Availability state of one persisted host workspace registration. */
public enum AuthorizedDirectoryStatus {
    ACTIVE,
    DISABLED,
    REVOKED
}
