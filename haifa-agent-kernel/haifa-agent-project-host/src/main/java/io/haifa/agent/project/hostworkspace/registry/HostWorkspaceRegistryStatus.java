package io.haifa.agent.project.hostworkspace.registry;

/** Availability state of one persisted host workspace registration. */
public enum HostWorkspaceRegistryStatus {
    ACTIVE,
    DISABLED,
    REVOKED
}
